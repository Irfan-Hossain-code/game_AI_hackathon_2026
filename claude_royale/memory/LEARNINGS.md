# LEARNINGS

- Parallel workers reduce effective action latency when model calls are slow.
- Battle detection should be conservative to avoid tapping menu buttons mid-match.
- Keep outputs strict JSON to avoid parser failures.
