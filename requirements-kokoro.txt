# Kokoro TTS (hexgrad) + deps. After pip install:
#   macOS: brew install espeak-ng
# Optional Apple Silicon GPU: export PYTORCH_ENABLE_MPS_FALLBACK=1
kokoro>=0.9.4
soundfile
torch
faster-whisper
sounddevice
numpy
pynput
